spring:
  application:
    name: url-shortener
  datasource:
    username: bahaa
    password: password
    url: jdbc:mysql://localhost:3306/urlDB
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true

shortener:
  code-length: 6
  max-tries: 10
  base-url: ${BASE_URL:http://localhost:8080}